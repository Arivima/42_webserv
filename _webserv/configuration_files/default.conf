# multi server

http {
    server {
    # IP
    host localhost;
    # port
    listen 8080;
    # domain name
    server_name example.com;

    # default directory where files should be searched
    root /var/www;
    
    # default error pages
    error_page 404 errors/404.html;
    error_page 500 502 503 504 errors/50x.html;

    # client body size limitation (range 1 byte - 1G) (syntax digit, K, M)
    body_size 8M;

    location /test_no_index {
    }
    # directory listing : list of all the files within directory requested
    autoindex on;
    
    # directory index : default file if url is a directory
    index html/index.html;
    
    # execution of cgi based on multiple file extension : .sh .pl .py
    cgi_enable /usr/bin/bash .sh /usr/bin/perl .pl /usr/bin/python3 .py;

    # ROUTES TO DIFFERENT DIRECTORIES -> location
    # test methods -> Postman
    location /test_get {
        autoindex off;
    }
    location /test_post {
        method POST;
    }
    location /test_delete {
        method DELETE;
    }
    location /test_delete_redirection {
        method DELETE;
    }
    location /test_notauthorized {
        method POST DELETE;
    }

    # test redirections
	location /old-page {
		method POST;
		return 308 http://127.0.0.1:8080/test_post/;
	}
	location /old-page-v2 {
		method DELETE;
		return http://127.0.0.1:8080/test_delete_redirection/;
	}

    # test CGI
    location /cgi-bin {
        method POST;
    }

    # test image upload and download
    location /gallery {
        body_size 500M;
        method POST;
    }
    }

# test multiple server with identical name, port, ip -> fails
    # server {
    # host 192.168.122.1;
    # listen 8086;
    # server_name example.it;
    # root /var/www;
    # index html/index.html;
    # autoindex on;

	# # LOCATION MATCHING TEST
	# # location /img {
	# # 	root /var/www/html;
	# # }

	# # location = /img/Doggie.jpg {
	# # 	root /var/www;
	# # }
	
	# # location /img/San {
	# # 	root /var/www/htmg;
	# # }

    # }

    server {
    host 192.168.122.1;
    listen 8086;
    server_name example.it;
    root /var/www;
    index html/index.html;
    autoindex on;
    }

    server {
    host 192.168.122.1;
    listen 8080;
    server_name example.fr;
    root /var/www;
    autoindex on;
    }

    server {
    host 192.168.122.1;
    listen 8085;
    server_name example.it;
    }

    server {
    host 192.168.122.1;
    listen 8085;
    server_name example.fr;
    }
    
}
